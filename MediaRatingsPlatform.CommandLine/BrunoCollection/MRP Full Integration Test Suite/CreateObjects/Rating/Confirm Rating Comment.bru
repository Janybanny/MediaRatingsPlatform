meta {
  name: Confirm Rating Comment
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/ratings/1/confirm
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
