meta {
  name: Login User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "user2",
    "password": "pass123"
  }
}

script:post-response {
  bru.setVar("token", res.body);
}

settings {
  encodeUrl: true
  timeout: 0
}
