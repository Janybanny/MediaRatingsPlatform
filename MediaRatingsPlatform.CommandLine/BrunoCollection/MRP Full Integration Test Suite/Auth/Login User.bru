meta {
  name: Login User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "user1",
    "password": "pass123"
  }
}

assert {
  res.status: eq 200
  res.body: length 36
}

script:post-response {
  bru.setVar("token", res.body);
}

settings {
  encodeUrl: true
  timeout: 0
}
