meta {
  name: Delete Media Entry
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/media/1
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
