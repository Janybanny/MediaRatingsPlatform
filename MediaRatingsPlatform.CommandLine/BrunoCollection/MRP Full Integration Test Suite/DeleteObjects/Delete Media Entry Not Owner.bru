meta {
  name: Delete Media Entry Not Owner
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/media/2
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token2}}
}

assert {
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
