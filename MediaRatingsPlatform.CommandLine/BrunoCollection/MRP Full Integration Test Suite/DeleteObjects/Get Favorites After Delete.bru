meta {
  name: Get Favorites After Delete
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/1/favorites
  body: none
  auth: inherit
}

params:query {
  : 
}

assert {
  res.status: eq 200
  res.body[0].Id: eq 2
  res.body[1]: isUndefined
}

settings {
  encodeUrl: true
  timeout: 0
}
