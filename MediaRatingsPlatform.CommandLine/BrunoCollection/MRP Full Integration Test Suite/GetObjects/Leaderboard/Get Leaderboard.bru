meta {
  name: Get Leaderboard
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/leaderboard
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body[0].User: eq user1
  res.body[1].User: eq user2
}

settings {
  encodeUrl: true
  timeout: 0
}
