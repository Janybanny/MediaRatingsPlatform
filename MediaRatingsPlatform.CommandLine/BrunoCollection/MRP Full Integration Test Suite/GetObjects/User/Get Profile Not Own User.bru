meta {
  name: Get Profile Not Own User
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users/2/profile
  body: none
  auth: inherit
}

assert {
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
