meta {
  name: Get Rating History
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/1/ratings
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body[0].Id: eq 1
  res.body[1].Id: eq 2
}

settings {
  encodeUrl: true
  timeout: 0
}
